[project]
name = "ts-agent"
version = "0.1.0"
description = "Python components for ts-agent"
requires-python = ">=3.12"
dependencies = [
    "ghat>=0.1.3",
    "matplotlib",
    "numpy",
    "browser-use",
    "playwright",
    "langchain-openai>=1.1.10",
]
