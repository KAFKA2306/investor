version: "3"

vars:
  TS_DIR: ts-agent
  DASHBOARD_DIR: ts-agent/src/dashboard

tasks:
  default:
    cmds:
      - task: check

  check:
    dir: "{{.TS_DIR}}"
    cmds:
      - bun run format
      - bun run lint
      - bun run typecheck

  run:
    dir: "{{.TS_DIR}}"
    cmds:
      - cmd: bun ./src/experiments/new_alpha_discovery.ts
      - cmd: bun ./src/pipeline/evaluate/foundation_benchmark.ts

  view:
    dir: "{{.DASHBOARD_DIR}}"
    cmds:
      - npm run dev